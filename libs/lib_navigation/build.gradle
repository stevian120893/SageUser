apply from: "$rootDir/gradle_scripts/lib.gradle"

dependencies {
    implementation "$appDependencies.kotlinStdlib"
    implementation "$appDependencies.sdp"
    api "$appDependencies.jetpackNavRuntime"
    api "$appDependencies.fragment"
}